<p id="notice"><%= notice %></p>

<h1>Products</h1>
<div id="add-product" class="container-fluid">
<% if signed_in? && current_user.admin? %>
 	<b><%= link_to 'Add New Product', new_product_path %></b>
<% end %>
</div>
<div class="container-fluid">
  <div class="row">
   <%= render @products %>
  </div> 
</div>

